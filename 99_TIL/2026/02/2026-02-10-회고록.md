# 2026년 2월 10일 (월) - 대규모 지연 버그 수정 완료 및 신규 이슈 발견

## 🎯 주요 성과
- fix/clinical-event-60s-delay 브랜치 dev 병합 완료 (16커밋, 9파일)
- 임상 이벤트 60초+ 지연 완벽 해결
- WebSocket 브로드캐스트 누락 원인 규명

## 📋 작업 내용

### 1. 완료된 수정 사항 (7개)

#### 1) 임상 이벤트 60초+ 지연 해결 ⭐
- **원인**: WCF 로그 버퍼링(20~33초) + 폴백 체인 블로킹
- **해결**: tshark 패킷에서 `Laerdal.Event.*` 직접 스캔
- **효과**: WCF 대기 없이 즉시 감지

#### 2) CLINICAL 파라미터 변경 즉시 감지
- 기흉, 폐음, 심음 등 임상 파라미터 tshark 직접 스캔
- WCF 경유 시 20~33초 지연 완전 우회

#### 3) STATE/SESSION/INITIAL_PARAM DB 저장
- 실시간 모니터링에는 보이지만 DB 미저장 문제 해결
- SESSION, STATE, 초기 파라미터 모두 정상 저장

#### 4) 체온(TEMP) 표시값 LLEAP TBlood 기준 통일
```csharp
// 이전: int? Temperature (정수값만)
// 개선: double? Temperature (소수점 지원)
// TPeri/TBlood 분리 저장으로 LLEAP 표시값과 완벽 일치
```

#### 5) 녹화 조기 종료 버그 수정
- **문제**: 초기화 stopScenario가 30초 후 도착하여 진짜 종료로 오인
- **해결**: 카운트 기반 stop 무시 로직 (초기화 stop 수만큼 이후 stop 무시)

#### 6) SimMan 초기 이벤트 80개 폭발 방지
- 배치 임계값(>3개) + 5초 쿨다운 + 3초 init window
- 초기 파라미터 덤프를 이벤트로 오인하는 문제 완전 차단

#### 7) tshark/WCF 크로스 파이프라인 중복 제거
- **문제**: 같은 이벤트가 양쪽에서 감지되어 2번 표시
- **해결**: 카운트 기반 dedup (tshark N건 → WCF N건 억제)

### 2. SIMPREC 수정 사항

#### 고스트 이벤트 제거 (commit: `6922bba`)
- 새 세션 시작 시 `app_state.room_events` 초기화
- 이전 세션 이벤트가 00:00:00 타임스탬프로 잔류하는 문제 해결

### 3. 심층 분석: WebSocket 브로드캐스트 누락

#### 문제 분석 과정
1. **증상**: 모니터링 페이지에 "REC 녹화 시작"만 표시, 임상 이벤트 간헐적 미표시
2. **조사 범위**:
   - SimMonitor C# 코드베이스 전체 분석
   - WebSocket 서버 생명주기
   - 이벤트 전달 경로
   - 세션 생명주기

#### 근본 원인 발견
```
SimMonitor → SIMPREC WebSocket으로 wcfEvent 메시지가 한 번도 수신된 적 없음
(바이탈/세션 정보는 정상 수신)

모든 임상 이벤트가 HTTP API(/events)로만 유입
↓
HTTP API는 DB 저장만 하고 프론트엔드 브로드캐스트 안 함
(2/5 중복 이벤트 수정 시 브로드캐스트 제거한 것이 원인)
```

#### 수정 계획
- HTTP API에서 브로드캐스트 복원
- `add_event()`에 timestamp+type+message 기반 2초 TTL dedup 로직 추가

### 4. 남은 문제 및 계획

| 문제 | 상태 | 계획 |
|---|---|---|
| 이벤트 반복 시 누락 | 테스트 필요 | tshark dedup 1초→100ms 단축 |
| 모니터링 페이지 50개 CLINICAL 로그 | 수정 필요 | 최초 파라미터로 병합 |
| 연속 클릭 미반영 | 수정 필요 | 이벤트 카운트 기반 처리 |

## 💡 핵심 교훈
1. **복잡한 시스템의 디버깅**: WebSocket과 HTTP API 두 경로를 모두 확인해야 함
2. **이전 수정의 부작용**: 2/5 중복 제거 시 브로드캐스트도 함께 제거된 것이 원인
3. **인수인계의 중요성**: 부재 시를 대비한 상세한 문서화 필수

## 🔄 개선 효과
| 항목 | 이전 | 개선 후 |
|---|---|---|
| 임상 이벤트 지연 | 60초+ | 즉시 ✅ |
| DB 저장 누락 | 일부 누락 | 완벽 저장 ✅ |
| 체온 정밀도 | 정수값 | 소수점 ✅ |
| 초기 이벤트 폭발 | 80개 | 0개 ✅ |

## 📝 인수인계 포인트
- WebSocket 브로드캐스트 복원 작업 필수
- 연속 클릭 이벤트 처리 로직 개선 필요
- 모니터링 페이지 초기 CLINICAL 로그 정리
- 테스트 시 LLEAP에서 반복 클릭 테스트 반드시 수행

## 🤔 회고
오늘은 정말 "수확의 날"이었다. 며칠 동안 씨름하던 60초 지연 문제를 완벽히 해결하고 dev 브랜치에 병합까지 완료했다. 특히 WebSocket 브로드캐스트 누락 문제의 근본 원인을 찾아낸 것이 큰 성과다. 이전에 중복 제거를 위해 수정했던 코드가 예상치 못한 부작용을 일으킨 것을 보며, 모든 수정은 신중해야 한다는 것을 다시 한번 깨달았다.

내일 출근하지 않는다는 부담감 때문에 인수인계를 특별히 신경 써서 작성했다. 저번처럼 내가 없는 동안 잘못된 작업이 진행되어 나중에 수습하느라 고생하고 싶지 않다. 아직 해결해야 할 문제들이 남아있지만, 오늘 하루는 정말 생산적이었다.