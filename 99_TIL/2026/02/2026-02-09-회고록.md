# 2026년 2월 9일 (일) - WCF 이벤트 지연 문제 대대적 개선

## 🎯 주요 성과
- 임상 이벤트 60초+ 지연 문제 해결
- tshark 직접 패킷 분석으로 SimMan 75초 지연 우회
- CLINICAL 타입 이벤트 모니터링 페이지 출력 오류 수정

## 📋 작업 내용

### 1. 주요 문제 현황
| 문제 | 상태 | 설명 |
|---|---|---|
| 이전 세션 값 잔존 | 진행 중 | 모니터링 페이지에 이전 세션 데이터 표시 |
| SimBaby 이벤트 지연 | 해결 중 | 이벤트 발생 → SimMonitor 전송 지연 심각 |
| SimMan 시나리오 종료 | 파악 중 | 시작 시 간헐적 자동 종료 (최초 1회만) |
| CLINICAL 타입 미출력 | ✅ 해결 | 모니터링 페이지 출력 오류 수정 완료 |

### 2. fix/clinical-event-60s-delay 브랜치 작업 상세

#### 완료된 커밋 (9개)

1. **`be60adb`** - 임상 이벤트 60초+ 지연 해결
   - 근본 원인 파악 및 수정

2. **`00de235`** - 다중 버그 동시 수정
   - 이중 세션 방지
   - 자동 종료 실패 수정
   - 잔여 stopScenario 버그 해결

3. **`4f3b6b4 + d36cc49`** - tshark 재시작 로직 (적용 후 Revert)
   - Disconnect 시 tshark 재시작 시도
   - 테스트 후 문제 발견으로 롤백

4. **`49abe51`** - SimMan WCF 이벤트 지연 개선
   - ForceFlush 200ms 적용으로 응답성 향상

5. **`d212e9d`** - tshark WCF Binary 직접 스캔 ⭐
   - SimMan ~75초 지연 우회 성공
   - 패킷 레벨에서 이벤트 직접 감지

6. **`9ea06c4`** - 초기 데이터 정리
   - SimMan 초기 덤프 필터링
   - 크로스파이프라인 중복 제거
   - 언더바 정리로 가독성 향상

7. **`cdfef87`** - 초기 이벤트 누출 차단
   - tshark 쿨다운 적용
   - WCF 초기 이벤트 윈도우로 덤프 누출 방지

8. **`8253e37`** - 데이터 무결성 개선
   - 고스트 이벤트 제거
   - STATE/SESSION/INITIAL_PARAM DB 저장

9. **`15b8df0`** - CLINICAL 파라미터 즉시 감지 ⭐
   - tshark에서 직접 감지 (WCF 20~33초 지연 우회)

#### 미완성/롤백된 작업 (3개)
- CLINICAL 크로스 파이프라인 중복 제거 (이중 dedup 키 적용)
- 초기 CLINICAL 데이터 파편화 수정 (배치 감지)
- NotifyParamChange() 초기화 윈도우 중 버퍼링

### 3. 혁신적 접근: tshark 직접 패킷 분석
```
기존 방식: 시뮬레이터 → LLEAP → WCF 로그 → SimMonitor
           (각 단계마다 지연 발생, 총 60~75초)

개선 방식: 시뮬레이터 → [tshark 직접 감지] → SimMonitor
           (중간 단계 우회, 즉시~수초 내 감지)
```

## 💡 핵심 교훈
1. **우회 전략의 효과**: 해결 불가능한 지연은 아예 다른 경로로 우회
2. **패킷 레벨 분석의 위력**: 네트워크 패킷 직접 분석으로 극적인 성능 개선
3. **점진적 개선 vs 근본적 재설계**: 때로는 근본적인 접근 변경이 필요

## 🔄 개선 효과
| 항목 | 이전 | 개선 후 |
|---|---|---|
| 임상 이벤트 지연 | 60초+ | 즉시~수초 ✅ |
| SimMan 이벤트 지연 | ~75초 | tshark 우회 ✅ |
| CLINICAL 타입 출력 | 미출력 | 정상 출력 ✅ |
| WCF 응답성 | 불규칙 | 200ms 주기 ✅ |

## 📝 남은 과제
- 이전 세션 데이터 완벽 클리어 로직 구현
- SimMan 간헐적 자동 종료 원인 규명
- 롤백된 3개 작업 재검토 및 안정화

## 🤔 회고
오늘은 정말 "근본적 전환"의 날이었다. 며칠 동안 WCF 로그 파일 읽기 지연을 줄이려고 노력했지만 한계가 있었는데, 아예 발상을 바꿔 tshark로 네트워크 패킷을 직접 분석하는 방법으로 전환하니 75초 지연이 거의 즉시 수준으로 개선되었다. 때로는 문제를 정면돌파하려 하지 말고 우회하는 것이 더 효과적이라는 교훈을 얻었다. 다만 하루에 9개나 되는 커밋을 작업하면서도 "제대로 작업된 게 맞나?"라는 의문이 드는 것은, 아마도 너무 많은 변경을 한 번에 시도했기 때문일 것이다. 앞으로는 더 작은 단위로 나누어 확실하게 검증하면서 진행해야겠다.